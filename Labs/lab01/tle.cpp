#include <bits/stdc++.h>

using namespace std;

int tc, n ,x;
int main() {
    //ios_base::sync_with_stdio(false); cin.tie(0);
    cin >> tc;
    while (tc--) {
        cin >> n;
        for(int i = 0; i < n; i++) {
            cin >> x;
        }
    }
}